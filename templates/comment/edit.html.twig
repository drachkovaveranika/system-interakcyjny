{% include 'default/_form.html.twig' with {
    page_title: 'Edit comment'|trans({'%id%': comment.id|default('')}),
    form: form,
    submit_label: 'edit'|trans,
    back_to_list_path: 'comment_index',
} %}