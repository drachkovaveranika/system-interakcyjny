{% include 'default/_form.html.twig' with {
    page_title: 'Edit'|trans({'%id%': catalog.id|default('')}),
    form: form,
    submit_label: 'edit'|trans,
    back_to_list_path: 'catalog_index',
} %}