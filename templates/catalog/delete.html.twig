{% include 'default/_form.html.twig' with {
    page_title: 'catalog delete'|trans({'%id%': catalog.id|default('')}),
    form: form,
    submit_label: 'delete'|trans,
    back_to_list_path: 'catalog_index',
} %}
